# pr-current command

`git-workflow-helper` エージェントを使用し、以下の方針でcommit・PRを行う。

## ブランチ作成

現在のブランチ及びデフォルトブランチに関わらず、現在のブランチから差分に合った名称でブランチを作成する。

## コミット先

新規作成したブランチにコミットを行う。

## PR

デフォルトブランチに関わらず、現在のブランチに対してPRを発行する。
